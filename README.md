# starstruct

starstruct is an webapp that lets you drop a project folder,  validates its structure against known Hack Club YSWS templates and returns a downloadable markdown tree and the result. It uses a vanilla HTML/CSS/JS frontend with a Python backend deployed as Vercel serverless functions.